Use to break the unfiltered archive into its three tiers; drop it when the segmented filter is active.

```jsx
<TierHeader label="Live markers" count={3} />
```

Sticks to the top of the scroller on `--panel`, so it never sits over a transparent row ground.
