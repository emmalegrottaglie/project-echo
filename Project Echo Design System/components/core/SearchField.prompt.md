Use as the archive's sticky search, and inside the receiver-directory sheet.

```jsx
<SearchField value={q} onChange={setQ} />
<SearchField value={q} onChange={setQ} placeholder="Search location" />
```

Monospace input, because what gets typed is usually a designator. 44px tall. There is no submit button and no debounce affordance — filtering is immediate.
