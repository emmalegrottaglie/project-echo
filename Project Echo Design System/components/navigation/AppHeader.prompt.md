Use at the top of every tab. It holds the wordmark and the theme control, nothing else.

```jsx
<AppHeader theme={theme} onThemeChange={setTheme} />
```

"PROJECT ECHO" set in uppercase mono, accent, 0.08em tracking — that is the brand mark; no logo exists and none should be drawn. Do not add navigation, a search icon or an avatar here.
