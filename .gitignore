node_modules/
dist/
server/data/
server/stream/
server/diagnostic/

# Capacitor: the android/ project is generated by `npx cap add android` and its
# build outputs are large. The APK is a build artefact, not source.
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
*.apk

*.local
.DS_Store

# The Claude Design bundle. Its tokens and fonts live in src/tokens/ and public/fonts/,
# which are now the source of truth; the bundle itself is a 3.8 MB design reference that
# nothing builds from and is kept locally rather than in the repository.
Project Echo Design System/

# Local agent/editor tooling (dev-server launch config).
.claude/
